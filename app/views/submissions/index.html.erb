<p id="notice"><%= notice %></p>

<h1>Submissions</h1>

<table>
  <thead>
    <tr>
      <th>Assignment</th>
      <th>Assignment title</th>
      <th>Original</th>
      <th>Original title</th>
      <th>Grade</th>
      <th>User</th>
      <th>Translation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @submissions.each do |submission| %>
      <tr>
        <td><%= submission.assignment_id %></td>
        <td><%= submission.assignment_title %></td>
        <td><%= submission.original_id %></td>
        <td><%= submission.original_title %></td>
        <td><%= submission.grade %></td>
        <td><%= submission.user_id %></td>
        <td><%= submission.translation_id %></td>
        <td><%= link_to 'Show', submission %></td>
        <td><%= link_to 'Edit', edit_submission_path(submission) %></td>
        <td><%= link_to 'Destroy', submission, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Submission', new_submission_path %>
