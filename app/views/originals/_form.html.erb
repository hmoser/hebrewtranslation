<%= form_for(original) do |f| %>
  <% if original.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(original.errors.count, "error") %> prohibited this original from being saved:</h2>

      <ul>
      <% original.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :upload %>
    <%= f.text_field :upload %>
  </div>

  <div class="field">
    <%= f.label :assignment_id %>
    <%= f.number_field :assignment_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
